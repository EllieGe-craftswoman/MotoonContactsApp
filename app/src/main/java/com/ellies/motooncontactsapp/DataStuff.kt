package com.ellies.motooncontactsapp

import androidx.lifecycle.MutableLiveData

/*
data class Contact(var name: String, var isFavourite: Boolean)

class ContactsRepo {
    var _contacts: MutableLiveData<List<Contact>> = MutableLiveData(mutableListOf())

    fun insert(contact: Contact){
        val newContacts = mutableListOf<Contact>()
        newContacts.add(contact)
        newContacts.addAll(_contacts.value!!)
        _contacts.postValue(newContacts)
    }

    fun getAll() = _contacts
}*/
